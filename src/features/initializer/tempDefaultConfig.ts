import { Config } from '../../api/config/defaultConfig';

const tempDefaultConfig: Config = {
  databaseDirectoryPath: '',
  theme: 'System',
  startWeekOn: 'Mon',
  style: 'Complete',
  _databaseFileName: 'data.sqlite3',
};

export default tempDefaultConfig;
